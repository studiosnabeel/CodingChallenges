function isLandscape(width, height) {
  // if (width > height) return true;
  // else return false;

  // return width > height ? true:false;

  //the preferred way of writing the code
  return width > height;
}

console.log(isLandscape(800, 400));
