//Write a function that takes two numbers and return the maximum of the two
let number = max(45, 35);

function max(num1, num2) {
  // if (num1 > num2) return num1;
  // return num2;

  //ternary operator
  return num1 > num2 ? num1 : num2;
}

console.log(number);
